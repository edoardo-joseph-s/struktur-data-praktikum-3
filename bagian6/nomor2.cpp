#include <iostream>
using namespace std;

int main() {
    int a1, a2, a3, a4, t;

    cout << "A: ";
    cin >> a1;
    cout << "B: ";
    cin >> a2;
    cout << "C: ";
    cin >> a3;
    cout << "D: ";
    cin >> a4;

    tambah = a1 + a2 + a3 + a4;

    cout << "Hasil dari A + B + C + D adalah: " << tambah << endl;

    return 0;
}
